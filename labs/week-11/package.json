{
  "name": "campus-service",
  "private": true,
  "description": "ENGSE203 Week 11 — ระบบ full-stack (starter)",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "check": "node --disable-warning=ExperimentalWarning check-week11.mjs",
    "build": "npm install --include=dev --prefix frontend && npm run build --prefix frontend",
    "start": "node --disable-warning=ExperimentalWarning api/src/server.js"
  },
  "devDependencies": {
    "supertest": "^7.2.2"
  }
}
